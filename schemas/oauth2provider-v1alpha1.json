{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://rashkash103.github.io/authentik-operator/schemas/oauth2provider-v1alpha1.json",
  "title": "OAuth2Provider (authentik.k8s.rka.sh/v1alpha1)",
  "description": "OAuth2Provider manages an OAuth2/OpenID Connect provider in authentik.",
  "x-generated-by": "Generated from config/crd/bases by hack/gen-schemas.py. Do not edit. CEL rules under x-kubernetes-validations are NOT enforced by editors - only the API server evaluates them.",
  "type": "object",
  "required": [
    "apiVersion",
    "kind",
    "spec"
  ],
  "properties": {
    "apiVersion": {
      "type": "string",
      "const": "authentik.k8s.rka.sh/v1alpha1",
      "description": "APIVersion of this resource. Always authentik.k8s.rka.sh/v1alpha1."
    },
    "kind": {
      "type": "string",
      "const": "OAuth2Provider",
      "description": "Kind of this resource. Always OAuth2Provider."
    },
    "metadata": {
      "type": "object"
    },
    "spec": {
      "description": "OAuth2ProviderSpec defines an OAuth2/OpenID Connect provider.",
      "properties": {
        "accessCodeValidity": {
          "description": "AccessCodeValidity in authentik duration syntax, e.g. \"minutes=1\".",
          "pattern": "^(((microseconds|milliseconds|seconds|minutes|hours|days|weeks)=-?\\d+);?)+$",
          "type": "string"
        },
        "accessTokenValidity": {
          "description": "AccessTokenValidity in authentik duration syntax, e.g. \"hours=1\".",
          "pattern": "^(((microseconds|milliseconds|seconds|minutes|hours|days|weeks)=-?\\d+);?)+$",
          "type": "string"
        },
        "adoptionPolicy": {
          "default": "FailOnConflict",
          "description": "AdoptionPolicy controls what happens when a provider with this name\nalready exists in authentik.",
          "enum": [
            "FailOnConflict",
            "AdoptExisting"
          ],
          "type": "string"
        },
        "authenticationFlow": {
          "description": "AuthenticationFlow is the flow used to authenticate a user who reaches\nthe application unauthenticated. Leave unset to use authentik's default.",
          "properties": {
            "name": {
              "description": "Name of the Flow resource.",
              "maxLength": 253,
              "minLength": 1,
              "type": "string"
            },
            "namespace": {
              "description": "Namespace holding the resource. Defaults to the referring resource's own\nnamespace.\n\nNaming another namespace requires the operator to be started with\ncross-namespace references enabled; otherwise the reference is refused\nrather than quietly resolved somewhere else.",
              "maxLength": 63,
              "type": "string"
            }
          },
          "required": [
            "name"
          ],
          "type": "object"
        },
        "authorizationFlow": {
          "description": "AuthorizationFlow is the flow used when authorizing this provider.",
          "properties": {
            "name": {
              "description": "Name of the Flow resource.",
              "maxLength": 253,
              "minLength": 1,
              "type": "string"
            },
            "namespace": {
              "description": "Namespace holding the resource. Defaults to the referring resource's own\nnamespace.\n\nNaming another namespace requires the operator to be started with\ncross-namespace references enabled; otherwise the reference is refused\nrather than quietly resolved somewhere else.",
              "maxLength": 63,
              "type": "string"
            }
          },
          "required": [
            "name"
          ],
          "type": "object"
        },
        "clientID": {
          "description": "ClientID to use. When empty authentik generates one.",
          "type": "string"
        },
        "clientSecretRef": {
          "description": "ClientSecretRef reads a fixed client secret from a Secret in this\nnamespace. When unset, authentik generates a secret.\n\nA secret is never accepted inline: putting one in a spec field would\nstore it in plain text in etcd and print it in `kubectl get -o yaml`.",
          "properties": {
            "key": {
              "description": "Key within the Secret's data.",
              "minLength": 1,
              "type": "string"
            },
            "name": {
              "description": "Name of the Secret.",
              "minLength": 1,
              "type": "string"
            }
          },
          "required": [
            "key",
            "name"
          ],
          "type": "object"
        },
        "clientType": {
          "default": "confidential",
          "description": "ClientType is confidential for clients that can keep a secret, public\nfor those that cannot (SPAs, native apps).",
          "enum": [
            "confidential",
            "public"
          ],
          "type": "string"
        },
        "connectionRef": {
          "description": "ConnectionRef selects the authentik instance this provider lives in.",
          "properties": {
            "kind": {
              "default": "AuthentikConnection",
              "description": "Kind of connection object being referenced.",
              "enum": [
                "AuthentikConnection",
                "ClusterAuthentikConnection"
              ],
              "type": "string"
            },
            "name": {
              "description": "Name of the connection object.",
              "minLength": 1,
              "type": "string"
            }
          },
          "required": [
            "name"
          ],
          "type": "object"
        },
        "deletionPolicy": {
          "default": "Delete",
          "description": "DeletionPolicy controls what happens to the authentik provider when this\nresource is deleted.",
          "enum": [
            "Delete",
            "Orphan"
          ],
          "type": "string"
        },
        "encryptionKeyPair": {
          "description": "EncryptionKeyPair is the certificate key pair used to encrypt tokens.\nWhen set, tokens are returned as JWEs.",
          "properties": {
            "name": {
              "description": "Name of the CertificateKeyPair resource.",
              "maxLength": 253,
              "minLength": 1,
              "type": "string"
            },
            "namespace": {
              "description": "Namespace holding the resource. Defaults to the referring resource's own\nnamespace.\n\nNaming another namespace requires the operator to be started with\ncross-namespace references enabled; otherwise the reference is refused\nrather than quietly resolved somewhere else.",
              "maxLength": 63,
              "type": "string"
            }
          },
          "required": [
            "name"
          ],
          "type": "object"
        },
        "grantTypes": {
          "description": "GrantTypes the provider will issue tokens for.",
          "items": {
            "description": "OAuth2GrantType is an OAuth2 grant the provider will issue tokens for.\n\nThis is a named type rather than a marker on the slice field because\ncontroller-gen applies an enum marker to the array itself in that case,\nproducing a schema that rejects every non-empty list.\n\nThe urn: values contain colons, which the marker parser reads as argument\nseparators unless each value is quoted.",
            "enum": [
              "authorization_code",
              "implicit",
              "hybrid",
              "refresh_token",
              "client_credentials",
              "password",
              "urn:ietf:params:oauth:grant-type:device_code",
              "urn:ietf:params:oauth:grant-type:token-exchange"
            ],
            "type": "string"
          },
          "type": "array"
        },
        "includeClaimsInIDToken": {
          "description": "IncludeClaimsInIDToken embeds scope claims in the id_token, for clients\nthat never call the userinfo endpoint.",
          "type": "boolean"
        },
        "invalidationFlow": {
          "description": "InvalidationFlow is the flow used when ending a session.",
          "properties": {
            "name": {
              "description": "Name of the Flow resource.",
              "maxLength": 253,
              "minLength": 1,
              "type": "string"
            },
            "namespace": {
              "description": "Namespace holding the resource. Defaults to the referring resource's own\nnamespace.\n\nNaming another namespace requires the operator to be started with\ncross-namespace references enabled; otherwise the reference is refused\nrather than quietly resolved somewhere else.",
              "maxLength": 63,
              "type": "string"
            }
          },
          "required": [
            "name"
          ],
          "type": "object"
        },
        "issuerMode": {
          "description": "IssuerMode selects how the issuer field is built.",
          "enum": [
            "global",
            "per_provider"
          ],
          "type": "string"
        },
        "logoutMethod": {
          "description": "LogoutMethod selects back-channel or front-channel logout.",
          "enum": [
            "backchannel",
            "frontchannel"
          ],
          "type": "string"
        },
        "logoutURI": {
          "description": "LogoutURI is called on logout.",
          "type": "string"
        },
        "name": {
          "description": "Name is the provider's name in authentik. Defaults to the resource name.",
          "type": "string"
        },
        "outpostRefs": {
          "description": "OutpostRefs are the outposts that should serve this provider.\n\nThe provider names the outposts rather than the outpost naming its\nproviders, so adding one provider is one manifest change. An outpost's\nmembership is reconciled additively: providers attached to it outside\nthis operator are left alone, and removing a reference here detaches only\nwhat this operator attached.",
          "items": {
            "description": "OutpostReference points at an Outpost resource.\n\nProviders name the outposts that should serve them, rather than an outpost\nlisting its providers. Membership therefore lives with the thing being added:\nonboarding an application edits that application's manifests and nothing\nelse, instead of also editing a shared outpost every team has to touch.",
            "properties": {
              "name": {
                "description": "Name of the Outpost resource.",
                "maxLength": 253,
                "minLength": 1,
                "type": "string"
              },
              "namespace": {
                "description": "Namespace holding the resource. Defaults to the referring resource's own\nnamespace.\n\nNaming another namespace requires the operator to be started with\ncross-namespace references enabled; otherwise the reference is refused\nrather than quietly resolved somewhere else.",
                "maxLength": 63,
                "type": "string"
              }
            },
            "required": [
              "name"
            ],
            "type": "object"
          },
          "type": "array"
        },
        "propertyMappings": {
          "description": "PropertyMappings attached to this provider, in order.",
          "items": {
            "description": "PropertyMappingReference points at a PropertyMapping resource in the same\nnamespace.",
            "properties": {
              "name": {
                "description": "Name of the PropertyMapping resource.",
                "maxLength": 253,
                "minLength": 1,
                "type": "string"
              },
              "namespace": {
                "description": "Namespace holding the resource. Defaults to the referring resource's own\nnamespace.\n\nNaming another namespace requires the operator to be started with\ncross-namespace references enabled; otherwise the reference is refused\nrather than quietly resolved somewhere else.",
                "maxLength": 63,
                "type": "string"
              }
            },
            "required": [
              "name"
            ],
            "type": "object"
          },
          "type": "array"
        },
        "redirectURIs": {
          "description": "RedirectURIs permitted for this client.",
          "items": {
            "description": "RedirectURI is one permitted OAuth2 redirect target.",
            "properties": {
              "matchingMode": {
                "default": "strict",
                "description": "MatchingMode selects exact or regular-expression matching.",
                "enum": [
                  "strict",
                  "regex"
                ],
                "type": "string"
              },
              "url": {
                "description": "URL is the redirect URI, or a regular expression matching one.",
                "minLength": 1,
                "type": "string"
              }
            },
            "required": [
              "url"
            ],
            "type": "object"
          },
          "type": "array"
        },
        "refreshTokenValidity": {
          "description": "RefreshTokenValidity in authentik duration syntax, e.g. \"days=30\".",
          "pattern": "^(((microseconds|milliseconds|seconds|minutes|hours|days|weeks)=-?\\d+);?)+$",
          "type": "string"
        },
        "signingKeyPair": {
          "description": "SigningKeyPair is the certificate key pair used to sign tokens.",
          "properties": {
            "name": {
              "description": "Name of the CertificateKeyPair resource.",
              "maxLength": 253,
              "minLength": 1,
              "type": "string"
            },
            "namespace": {
              "description": "Namespace holding the resource. Defaults to the referring resource's own\nnamespace.\n\nNaming another namespace requires the operator to be started with\ncross-namespace references enabled; otherwise the reference is refused\nrather than quietly resolved somewhere else.",
              "maxLength": 63,
              "type": "string"
            }
          },
          "required": [
            "name"
          ],
          "type": "object"
        },
        "subMode": {
          "description": "SubMode selects what the `sub` claim contains.",
          "enum": [
            "hashed_user_id",
            "user_id",
            "user_uuid",
            "user_username",
            "user_email",
            "user_upn"
          ],
          "type": "string"
        },
        "writeCredentialsTo": {
          "description": "WriteCredentialsTo creates a Secret holding the client id and secret, so\nthe workload that needs them can mount it. This is usually the point of\ncreating the provider in the first place.",
          "properties": {
            "clientIDKey": {
              "default": "client-id",
              "description": "ClientIDKey is the Secret key holding the client id.",
              "type": "string"
            },
            "clientSecretKey": {
              "default": "client-secret",
              "description": "ClientSecretKey is the Secret key holding the client secret.",
              "type": "string"
            },
            "issuerKey": {
              "default": "issuer",
              "description": "IssuerKey optionally holds the provider's issuer URL, which most OIDC\nclients need alongside the credentials.",
              "type": "string"
            },
            "name": {
              "description": "Name of the Secret to create in this resource's namespace.",
              "minLength": 1,
              "type": "string"
            }
          },
          "required": [
            "name"
          ],
          "type": "object"
        }
      },
      "required": [
        "authorizationFlow",
        "connectionRef",
        "invalidationFlow"
      ],
      "type": "object"
    },
    "status": {
      "description": "OAuth2ProviderStatus reports the provider's state in authentik.",
      "properties": {
        "adopted": {
          "description": "Adopted records that this resource took over a pre-existing authentik\nobject rather than creating it.",
          "type": "boolean"
        },
        "authentikURL": {
          "description": "AuthentikURL is the instance this object was reconciled against.\n\nA reference from another namespace compares this against its own\nconnection and refuses a mismatch. It matters more here than for a UUID:\nproviders are keyed by a small integer, so a primary key from a different\nauthentik will very likely name some other provider rather than fail.",
          "type": "string"
        },
        "clientID": {
          "description": "ClientID currently configured in authentik. The client id is not a\ncredential on its own, so it is safe to surface; the secret never is.",
          "type": "string"
        },
        "conditions": {
          "description": "Conditions describe the current state of the resource.",
          "items": {
            "description": "Condition contains details for one aspect of the current state of this API Resource.",
            "properties": {
              "lastTransitionTime": {
                "description": "lastTransitionTime is the last time the condition transitioned from one status to another.\nThis should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.",
                "format": "date-time",
                "type": "string"
              },
              "message": {
                "description": "message is a human readable message indicating details about the transition.\nThis may be an empty string.",
                "maxLength": 32768,
                "type": "string"
              },
              "observedGeneration": {
                "description": "observedGeneration represents the .metadata.generation that the condition was set based upon.\nFor instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date\nwith respect to the current state of the instance.",
                "format": "int64",
                "minimum": 0,
                "type": "integer"
              },
              "reason": {
                "description": "reason contains a programmatic identifier indicating the reason for the condition's last transition.\nProducers of specific condition types may define expected values and meanings for this field,\nand whether the values are considered a guaranteed API.\nThe value should be a CamelCase string.\nThis field may not be empty.",
                "maxLength": 1024,
                "minLength": 1,
                "pattern": "^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$",
                "type": "string"
              },
              "status": {
                "description": "status of the condition, one of True, False, Unknown.",
                "enum": [
                  "True",
                  "False",
                  "Unknown"
                ],
                "type": "string"
              },
              "type": {
                "description": "type of condition in CamelCase or in foo.example.com/CamelCase.",
                "maxLength": 316,
                "pattern": "^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$",
                "type": "string"
              }
            },
            "required": [
              "lastTransitionTime",
              "message",
              "reason",
              "status",
              "type"
            ],
            "type": "object"
          },
          "type": "array",
          "x-kubernetes-list-map-keys": [
            "type"
          ],
          "x-kubernetes-list-type": "map"
        },
        "credentialsRotatedAt": {
          "description": "CredentialsRotatedAt records the last client secret rotation.",
          "format": "date-time",
          "type": "string"
        },
        "credentialsSecretName": {
          "description": "CredentialsSecretName is the Secret the credentials were written to.",
          "type": "string"
        },
        "lastSyncedTime": {
          "description": "LastSyncedTime is when the resource last reconciled successfully.",
          "format": "date-time",
          "type": "string"
        },
        "observedGeneration": {
          "description": "ObservedGeneration is the .metadata.generation this status reflects.",
          "format": "int64",
          "type": "integer"
        },
        "observedRotationToken": {
          "description": "ObservedRotationToken is the value of the rotation annotation that was\nlast acted on. A rotation happens when the annotation differs from this,\nwhich makes the trigger idempotent: re-reconciling the same resource\ncannot rotate the secret again and break running workloads.",
          "type": "string"
        },
        "providerID": {
          "description": "ProviderID is authentik's numeric primary key for this provider. It is\nwhat an Application or Outpost must reference, so it is surfaced\nseparately from the generic RemoteID string.",
          "format": "int32",
          "type": "integer"
        },
        "remoteID": {
          "description": "RemoteID is authentik's own identifier for the managed object. Its shape\ndiffers by kind: a numeric primary key for providers, the slug for an\napplication, and a UUID for everything else.\n\nOnce set this is the authoritative handle for the object. Lookups prefer\nit over the name, so that renaming the object on either side does not\ncause the operator to lose track of it and create a duplicate.",
          "type": "string"
        },
        "remoteName": {
          "description": "RemoteName is the name or slug last observed in authentik. Informational.",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  "additionalProperties": false
}
