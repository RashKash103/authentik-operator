{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://rashkash103.github.io/authentik-operator/schemas/dockerserviceconnection-v1alpha1.json",
  "title": "DockerServiceConnection (authentik.k8s.rka.sh/v1alpha1)",
  "description": "DockerServiceConnection manages a Docker outpost service connection in\nauthentik.",
  "x-generated-by": "Generated from config/crd/bases by hack/gen-schemas.py. Do not edit. CEL rules under x-kubernetes-validations are NOT enforced by editors - only the API server evaluates them.",
  "type": "object",
  "required": [
    "apiVersion",
    "kind",
    "spec"
  ],
  "properties": {
    "apiVersion": {
      "type": "string",
      "const": "authentik.k8s.rka.sh/v1alpha1",
      "description": "APIVersion of this resource. Always authentik.k8s.rka.sh/v1alpha1."
    },
    "kind": {
      "type": "string",
      "const": "DockerServiceConnection",
      "description": "Kind of this resource. Always DockerServiceConnection."
    },
    "metadata": {
      "type": "object"
    },
    "spec": {
      "description": "DockerServiceConnectionSpec defines a Docker service connection.\n\nEither authentik talks to the Docker socket it is mounted with, in which\ncase local is set, or it dials an explicit URL. Setting both is rejected,\nbecause it hides which of the two endpoints is actually in use.",
      "properties": {
        "adoptionPolicy": {
          "default": "FailOnConflict",
          "description": "AdoptionPolicy controls what happens when a service connection with this\nname already exists in authentik.",
          "enum": [
            "FailOnConflict",
            "AdoptExisting"
          ],
          "type": "string"
        },
        "connectionRef": {
          "description": "ConnectionRef selects the authentik instance this service connection\nlives in.",
          "properties": {
            "kind": {
              "default": "AuthentikConnection",
              "description": "Kind of connection object being referenced.",
              "enum": [
                "AuthentikConnection",
                "ClusterAuthentikConnection"
              ],
              "type": "string"
            },
            "name": {
              "description": "Name of the connection object.",
              "minLength": 1,
              "type": "string"
            }
          },
          "required": [
            "name"
          ],
          "type": "object"
        },
        "deletionPolicy": {
          "default": "Delete",
          "description": "DeletionPolicy controls what happens to the authentik service connection\nwhen this resource is deleted.",
          "enum": [
            "Delete",
            "Orphan"
          ],
          "type": "string"
        },
        "local": {
          "description": "Local makes authentik use the Docker socket mounted into its own\ncontainer instead of dialling a URL.",
          "type": "boolean"
        },
        "name": {
          "description": "Name is the service connection's name in authentik. Defaults to the\nresource name.",
          "type": "string"
        },
        "tlsAuthentication": {
          "description": "TLSAuthentication is the certificate key pair presented to the Docker daemon\nas a client certificate.",
          "properties": {
            "name": {
              "description": "Name of the CertificateKeyPair resource.",
              "maxLength": 253,
              "minLength": 1,
              "type": "string"
            },
            "namespace": {
              "description": "Namespace holding the resource. Defaults to the referring resource's own\nnamespace.\n\nNaming another namespace requires the operator to be started with\ncross-namespace references enabled; otherwise the reference is refused\nrather than quietly resolved somewhere else.",
              "maxLength": 63,
              "type": "string"
            }
          },
          "required": [
            "name"
          ],
          "type": "object"
        },
        "tlsVerification": {
          "description": "TLSVerification is the certificate key pair holding the CA used to verify\nthe Docker daemon's certificate.",
          "properties": {
            "name": {
              "description": "Name of the CertificateKeyPair resource.",
              "maxLength": 253,
              "minLength": 1,
              "type": "string"
            },
            "namespace": {
              "description": "Namespace holding the resource. Defaults to the referring resource's own\nnamespace.\n\nNaming another namespace requires the operator to be started with\ncross-namespace references enabled; otherwise the reference is refused\nrather than quietly resolved somewhere else.",
              "maxLength": 63,
              "type": "string"
            }
          },
          "required": [
            "name"
          ],
          "type": "object"
        },
        "url": {
          "description": "URL of the Docker daemon, either \"unix:///var/run/docker.sock\" for a\nlocal socket or \"https://hostname:2376\" for a remote daemon.",
          "type": "string"
        }
      },
      "required": [
        "connectionRef"
      ],
      "type": "object",
      "x-kubernetes-validations": [
        {
          "message": "set exactly one of local: true or url",
          "rule": "(has(self.local) && self.local) != (has(self.url) && self.url.size() > 0)"
        }
      ]
    },
    "status": {
      "description": "DockerServiceConnectionStatus reports the connection's state in authentik.",
      "properties": {
        "adopted": {
          "description": "Adopted records that this resource took over a pre-existing authentik\nobject rather than creating it.",
          "type": "boolean"
        },
        "authentikURL": {
          "description": "AuthentikURL is the instance this object was reconciled against.\n\nA reference from another namespace compares this against its own\nconnection and refuses a mismatch. It matters more here than for a UUID:\nproviders are keyed by a small integer, so a primary key from a different\nauthentik will very likely name some other provider rather than fail.",
          "type": "string"
        },
        "conditions": {
          "description": "Conditions describe the current state of the resource.",
          "items": {
            "description": "Condition contains details for one aspect of the current state of this API Resource.",
            "properties": {
              "lastTransitionTime": {
                "description": "lastTransitionTime is the last time the condition transitioned from one status to another.\nThis should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.",
                "format": "date-time",
                "type": "string"
              },
              "message": {
                "description": "message is a human readable message indicating details about the transition.\nThis may be an empty string.",
                "maxLength": 32768,
                "type": "string"
              },
              "observedGeneration": {
                "description": "observedGeneration represents the .metadata.generation that the condition was set based upon.\nFor instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date\nwith respect to the current state of the instance.",
                "format": "int64",
                "minimum": 0,
                "type": "integer"
              },
              "reason": {
                "description": "reason contains a programmatic identifier indicating the reason for the condition's last transition.\nProducers of specific condition types may define expected values and meanings for this field,\nand whether the values are considered a guaranteed API.\nThe value should be a CamelCase string.\nThis field may not be empty.",
                "maxLength": 1024,
                "minLength": 1,
                "pattern": "^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$",
                "type": "string"
              },
              "status": {
                "description": "status of the condition, one of True, False, Unknown.",
                "enum": [
                  "True",
                  "False",
                  "Unknown"
                ],
                "type": "string"
              },
              "type": {
                "description": "type of condition in CamelCase or in foo.example.com/CamelCase.",
                "maxLength": 316,
                "pattern": "^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$",
                "type": "string"
              }
            },
            "required": [
              "lastTransitionTime",
              "message",
              "reason",
              "status",
              "type"
            ],
            "type": "object"
          },
          "type": "array",
          "x-kubernetes-list-map-keys": [
            "type"
          ],
          "x-kubernetes-list-type": "map"
        },
        "lastSyncedTime": {
          "description": "LastSyncedTime is when the resource last reconciled successfully.",
          "format": "date-time",
          "type": "string"
        },
        "observedGeneration": {
          "description": "ObservedGeneration is the .metadata.generation this status reflects.",
          "format": "int64",
          "type": "integer"
        },
        "remoteID": {
          "description": "RemoteID is authentik's own identifier for the managed object. Its shape\ndiffers by kind: a numeric primary key for providers, the slug for an\napplication, and a UUID for everything else.\n\nOnce set this is the authoritative handle for the object. Lookups prefer\nit over the name, so that renaming the object on either side does not\ncause the operator to lose track of it and create a duplicate.",
          "type": "string"
        },
        "remoteName": {
          "description": "RemoteName is the name or slug last observed in authentik. Informational.",
          "type": "string"
        },
        "serviceConnectionID": {
          "description": "ServiceConnectionID is authentik's UUID for this service connection. It\nis what an Outpost's serviceConnectionRef ultimately resolves to, so it\nis surfaced separately from the generic RemoteID string.",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  "additionalProperties": false
}
