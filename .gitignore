

# [epiq]: local hydrated state is never committed
.epiq/events/
.epiq/log/

# Build output
bin/
dist/
testbin/
*.test

# Zensical documentation build output
site/
.cache/

# Coverage
cover.out
coverage.*

# Local environment / secrets
.env
.env.local
*.local.yaml
kubeconfig

# Claude Code local state. The skills under .claude/skills/ are checked in -
# they are repeatable procedures for this repository, not personal settings -
# so ignore only the parts that are genuinely local.
.claude/worktrees/
.claude/settings.local.json

# Editor
.idea/
.vscode/
*.swp

# Python bytecode from the hack/ generators.
__pycache__/
